// let marks = 70;

// if (marks > 40) {
//   console.log("You have passed the test");
// }

// if (marks > 60) {
//   console.log("You have goat an A")
// }

// else {
//   console.log("You have failed the test");
// }

// isPrimeMember = false;

// if (isPrimeMember) {
//   console.log("You have access to all the premium content")
// }

// if (!isPrimeMember) {
//   console.log("Please buy a premium membership")
// }

// Disney + Hotstar.

let isPremiumMember = true;
let isDiscoveryMember = false;
let isAccornMember = false;

if (isPremiumMember) {
  if (isDiscoveryMember) {
    if (isAccornMember) {
      console.log("You have an access to Hotstar + Accorn + Discovery content");
    }
    else {
      console.log("You have access to Hotstar + Discovery content");
    }
  }
  else {
    console.log("You have access to Hotstar content only");
  }
}
else {
  console.log("Please buy a membership");
}

/* Q. WAP number n divisible by the number m?
Q. WAP to find out if number n is a multiple of number m.
Q. WAP to find out whether valid voters or not?
Q. WAP finds out if any year is a leap year or not.
Q. WAP to check if any given number is 1 digit or not?
Q. WAP to check if any given number is 2 digit or not?
Q. WAP to check if any given number is 3 digit or not?

Q. WAP to return the absolute number of a given input
Example:
Input1 - 15
Output - 15
Input2 - -4
Output - 4
Q. WAP to round off the decimal value and get an only integer value.
Example:
Input1 - 15.3
Output - 15
Input2 - 25.6
Output - 26

Q. WAP to get the ceiling number from the given decimal (without roundoff, ceiling value).
Example:
Input1 - 15.7
Output - 16
Input2 - 25.1
Output - 26

Q. WAP to do the following things as for input n number:
-> if even, then add 2.
-> if odd, then add 1.
-> if odd, then add 1.

Q. WAP to calculate the commission of sale person as follow:
1. sale is greater than 10000 -- 5%
2. sale is in between 8k-10 -- 4%
3. sale is in between 5k-8k -- 3%
4. sale is less than 5k -- 2%

Q. WAP to return the count of digits for an input.
Example
Input - 123
Output - 3

Q. WAP to return the sum of digits for an input.
Example
Input - 123
Output - 6

Q. WAP to return the multiplication of digits for an input.
Example
Input - 1234
Output - 24

Q. WAP to return the reserve the number 
Example
Input - 123
Output - 321

*/